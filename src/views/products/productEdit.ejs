<%- include('../partials/head', {title: 'Editar'}) %>
<link rel="stylesheet" href="/css/products-edit.css">
<title>Crear Producto</title>
</head>
<body>
<%- include('../partials/header') %>
<main>
    <div class="producto-crear"> 
        <div class="producto-detail">
            <h3 class="titulo-register">
                <label for="h3">Editar Producto: </label>  
                <span name="h3"> <%= productos.titulo%></span>
            </h3>
            <div class="info-register">
                <form action="/products/<%= producto.id %>?_method=PUT" method="POST" enctype="multipart/form-data">
                    <span>Nombre del producto:</span> 
                    <br>
                    <input type="text" name= "titulo" class="nombre-producto" value="<%= producto.titulo %>">
                    <% if(locals.errors && errors.titulo) { %>
                        <p class= "errores"> <%= errors.titulo.msg %> </p>
                    <% } %> 
                    <br>
                    <span>Descripcion del producto:</span>
                    <br>
                    <input type="text" name= "descripcion" class="descripcion-producto" value="<%= producto.descripcion %>"> 
                    <br>
                    <span>Marca: </span>
                    <br>
                    <input type="text" name="marca" class="marca-producto" value="<%= producto.marca %>">
                    <br>
                    <span>Modelo: </span>
                    <br>
                    <input type="text" name="modelo" class="modelo-producto" value="<%= producto.modelo %>">                                                  
                    <br>
                    <div class="foto-producto">
                        <label for="file-upload" class="custom-file"><i class="fa fa-cloud-upload"></i> Ingrese la foto del producto</label>
                        <input type="file" name="img">
                    </div>
                    </div class="categoria">
                        <span>Seleccione la categoria del producto:</span>
                        <br>
                        <div class="div-radio">
                        <input type="radio" id="Motherboard" name="tipo" value="Motherboard" <% if (producto.tipo == 'Motherboard') { %> 
                            checked
                            <% } %> >
                        <label for="Motherboard">Motherboard</label>
                        <br>
                        <input type="radio" id="Procesador" name="tipo" value="Procesador" <% if (producto.tipo == 'Procesador') { %> 
                            checked
                            <% } %> >
                        <label for="Procesador">Procesador</label>
                        <br>
                        <input type="radio" id="GPU" name="tipo" value="GPU" <% if (producto.tipo == 'GPU') { %> 
                            checked
                            <% } %> >
                        <label for="GPU">GPU</label>
                        <br> 
                        <input type="radio" id="RAM" name="tipo" value="RAM" <% if (producto.tipo == 'RAM') { %> 
                            checked
                            <% } %> >
                        <label for="RAM">RAM</label>
                        <br>
                    </div>
                    <br>
                    <div class="descuento-promo">
                        <span>Aplicar promocion: </span>
                        <select name="enPromocion" id="enPromocion">
                            <option value="1"<% if(producto.enPromocion==true){%>
                                selected
                                <%}%>>Si</option>
                            <option value="0"<% if(producto.enPromocion==false){%>
                                selected
                                <%}%>>No</option>
                        </select>
                        <br>
                        <span>Porcentaje (%) de descuento</span>
                        <input type="text" name="descuento" class="descuento" value="<%=producto.descuento*100%>">
                    </div>
                    <div class="precio">
                        <span>Precio del producto:</span>
                        <br>
                        <div class="moneda-precio">
                            <select name="moneda" id="moneda">
                                <option value="USD"<% if(producto.moneda=="USD"){%>
                                    selected
                                    <%}%>>USD</option>
                                <option value="ARS"<% if(producto.moneda=="ARS"){%>
                                    selected
                                    <%}%>>ARS</option>
                            </select>
                            <input type="number" name="precio" class="precio-producto"value="<%=producto.precio %>">
                        </div> 
                    </div>
                    <div class="boton-agregar-div"> 
                        <button type="submit" class="boton-agregar">Guardar</button>
                    </div>
                </form>
            </div>
        </div>
    </div>
</main>
<%- include('../partials/footer') %>
</body>
</html>